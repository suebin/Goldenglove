<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.TeamDAO">
	
	<!-- 팀원 등록 -->
	<insert id="insertTeam" parameterType="teamDTO">
		insert into team values(#{teamName}, #{firstBase}, #{secondBase}, #{thirdBase}, #{catcher}, #{pitcher}, #{leftFielder}, #{rightFielder}, #{centerFielder}, #{shortStop})
	</insert>
	
	<!-- 팀원 조회 -->
	<select id="selectTeam" resultType="teamDTO" parameterType="String">
		select * from team where teamName = #{teamName};
	</select>
	
	<!-- 수정 -->
	<update id="updateTeam" parameterType="teamDTO">
		update team set firstBase = #{firstBase}, secondBase = #{secondBase}, thirdBase = #{thirdBase}, catcher = #{catcher}, pitcher = #{pitcher}, leftFielder = #{leftFielder}, rightFielder = #{rightFielder}, centerFielder = #{centerFielder}, shortStop = #{shortStop} where teamName = #{teamName};
	</update>
</mapper>