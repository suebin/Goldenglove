<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="teammatch.TeamMatchDAO">

	<insert id="insertTeamMatch" parameterType="teammatchdto">
		insert into teammatching (teamName, region, possibleDate, possibleTime, homePlace, headCount, comment)
		values(#{teamName}, #{region}, #{possibleDate}, #{possibleTime}, #{homePlace}, #{headCount}, #{comment})
	</insert>

</mapper>

