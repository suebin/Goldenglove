<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="alarm.AlarmDAO">
	<select id="selectAlarm" resultType="alarmDTO">
		select * from alarm where homeName = #{homeName} and awayName IS NOT NULL and alarmDate >= DATE_SUB(NOW(), INTERVAL 7 DAY);
	</select>

	<update id="checkAlarm" parameterType="int">
		update alarm set checked = 1 where seq = #{seq}
	</update>
</mapper>